String tekst = "Met een gewicht van ";
String tekst_2 = " kg en een lengte van ";
String tekst_3 = " cm, is jouw BMI ";
float gewicht = 67.6;
float lengte = 1.70;

float bmi = gewicht / (lengte * lengte);
 println (tekst + gewicht + tekst_2 + lengte + tekst_3 + bmi);
